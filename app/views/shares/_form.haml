= form_with(model: share, local: true) do |form|

  .field
    = form.label :title
    = form.text_field :title
  

  .field
    = form.label :video
    = form.file_field :video, :accept => 'video/*'

  .actions
    = form.submit
